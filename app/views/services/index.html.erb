<div class="wrapper">

	<div class="row push-top">
		<div class="c8">
			<% @services.each do |service| %>
				<h1><%= link_to "#{service.name}", service_path(service) %></h1>
				<p><%= service.description.html_safe %></p>
			<% end %>
			<%= link_to "Add a service", new_service_path, class: "button" %>
		</div>
	</div>
</div>