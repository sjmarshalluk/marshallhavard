<div class="wrapper">

	<div class="row push-top">
		<div class="c8">
			
			<% @clients.each do |client| %>
				<h1><%= link_to "#{client.title}", client_path(client) %></h1>
			<% end %>

		</div>
	</div>
</div>