<div class="wrapper">

	<div class="row push-top">
		<div class="c8">
			<h1>Edit <em><%= @guide.title %></em></h1>
			<%= render "form" %>

			<%= link_to "Delete '#{@guide.title}'", guide_path(@guide), method: :delete, class: "button danger" %>
		</div>
	</div>
</div>

<%= tinymce :content_css => asset_path('application.css')%>